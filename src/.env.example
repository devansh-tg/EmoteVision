# EmoteVision — environment variable reference
# Copy this file to .env and fill in values as needed.
# Loaded automatically by python-dotenv (app.py calls load_dotenv()).
#
# None of these are required — sensible defaults are used when unset.

# ── Server ───────────────────────────────────────────────────────────────────
EMOTEVISION_HOST=0.0.0.0        # Bind address (default: 0.0.0.0)
EMOTEVISION_PORT=5000            # HTTP port     (default: 5000)
EMOTEVISION_DEBUG=false          # Flask debug   (default: false)

# ── Model paths ──────────────────────────────────────────────────────────────
EMOTEVISION_MODEL=model.h5       # Keras .h5 model  (relative to src/)
EMOTEVISION_TFLITE=model.tflite  # TFLite model     (relative to src/)
EMOTEVISION_META=model_meta.json # Training metadata JSON

# ── Inference backend ────────────────────────────────────────────────────────
# auto  → use TFLite if model.tflite exists, otherwise fall back to Keras
# true  → always use TFLite (raises error if model.tflite missing)
# false → always use Keras (.h5)
USE_TFLITE=auto

# ── Camera ───────────────────────────────────────────────────────────────────
CAMERA_INDEX=0                   # OpenCV camera index (0 = default webcam)
GUI_CAMERA_WIDTH=640             # Desktop GUI capture width
GUI_CAMERA_HEIGHT=480            # Desktop GUI capture height
